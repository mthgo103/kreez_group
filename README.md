# موقع مجموعة كريز للصناعة

موقع ويب احترافي ورسمي لمجموعة كريز للصناعة، متخصصة في إنتاج العبوات المعدنية والبلاستيكية للمواد الغذائية.

## 🌐 الصفحات الرئيسية

- **الرئيسية** - عرض جذاب لمجموعة كريز مع المصانع والمميزات
- **المنتجات** - عرض شامل للعبوات المعدنية والبلاستيكية
- **نبذة عنا** - معلومات الشركة والمصانع والقيم الأساسية
- **اتصل بنا** - نموذج اتصال وخريطة وبيانات التواصل

## 🛠️ التقنيات المستخدمة

- **React 19** - مكتبة واجهات المستخدم
- **TypeScript** - لغة البرمجة
- **Tailwind CSS 4** - نظام التصميم
- **Vite** - أداة البناء السريعة
- **Wouter** - مكتبة التوجيه

## 📦 المتطلبات

- Node.js 18+
- pnpm (أو npm/yarn)

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات
```bash
pnpm install
```

### 2. تشغيل الموقع محلياً
```bash
pnpm dev
```
سيفتح الموقع على `http://localhost:5173`

### 3. بناء للإنتاج
```bash
pnpm build
```
ستجد الملفات الجاهزة في مجلد `dist/`

## 📤 النشر على GitHub Pages

### الخطوة 1: إنشاء Repository
1. اذهب إلى [github.com](https://github.com)
2. أنشئ Repository جديد باسم `kreez_group`
3. اختر **Public**

### الخطوة 2: رفع الملفات
```bash
git init
git add .
git commit -m "Initial commit: Kreez Group website"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/kreez_group.git
git push -u origin main
```

### الخطوة 3: تفعيل GitHub Pages
1. اذهب إلى **Settings** في مستودعك
2. اختر **Pages** من القائمة الجانبية
3. اختر:
   - **Source**: Deploy from a branch
   - **Branch**: main
   - **Folder**: /dist
4. اضغط **Save**

### الخطوة 4: الانتظار والتحقق
- انتظر 2-3 دقائق
- الموقع سيكون متاحاً على: `https://YOUR_USERNAME.github.io/kreez_group`

## 🔄 التحديث التلقائي (اختياري)

أنشئ ملف `.github/workflows/deploy.yml` لأتمتة النشر:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm build
      - uses: actions/upload-pages-artifact@v2
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v2
        id: deployment
```

## 📝 تعديل المحتوى

### تغيير بيانات الاتصال
عدّل الملفات التالية:
- `client/src/components/Footer.tsx` - معلومات التذييل
- `client/src/pages/Contact.tsx` - صفحة الاتصال

### تغيير الألوان
عدّل `client/src/index.css` وابحث عن:
- `--primary: #003D82` - اللون الأساسي
- `--secondary: oklch(0.5 0.08 240)` - اللون الثانوي

### تغيير الشعار
استبدل `client/public/logo.png` بشعارك الجديد

## 📂 بنية المشروع

```
kreez_group/
├── client/
│   ├── public/
│   │   └── logo.png
│   ├── src/
│   │   ├── components/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── ...
│   │   ├── pages/
│   │   │   ├── Home.tsx
│   │   │   ├── Products.tsx
│   │   │   ├── About.tsx
│   │   │   ├── Contact.tsx
│   │   │   └── NotFound.tsx
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   └── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

## 📞 معلومات الاتصال

- **الهاتف**: +966 50 123 4567
- **البريد**: info@kreezgroup.com
- **العنوان**: الرياض، المملكة العربية السعودية

## 📄 الترخيص

جميع الحقوق محفوظة لمجموعة كريز للصناعة © 2024

## 🤝 المساهمة

للمساهمة في تحسين الموقع، يرجى إنشاء Pull Request مع وصف التغييرات.

---

**ملاحظة**: بيانات الاتصال الحالية تجريبية ويمكن تعديلها حسب احتياجاتك.
